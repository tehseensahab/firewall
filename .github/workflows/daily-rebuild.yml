```yaml name: Daily rebuild for scheduled posts on: schedule: - cron: "15 9 * * *" workflow_dispatch: {} jobs: trigger-rebuild: runs-on: ubuntu-latest steps: - name: Call Cloudflare Pages deploy hook run: curl -X POST "${{ secrets.CLOUDFLARE_DEPLOY_HOOK_URL }}" ```
